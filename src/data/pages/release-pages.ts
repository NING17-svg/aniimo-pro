import type { PageContent } from "@/types/content";

// Site Plan declares no /release-date or /faq hub. The release-overview fixed
// page carries all launch status. FAQ is supplied inline per page.
export const releasePages: PageContent[] = [];
